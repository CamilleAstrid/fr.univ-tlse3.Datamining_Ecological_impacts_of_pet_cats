---
title: "Vignette Preparator et PreparatorQuantitativ"
author: "AMOUROUX Jan RODRIGES Camille-Astrid"
date: "`r Sys.Date()`"
output: 
  html_document: 
    fig_width: 8
    fig_height: 6
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(fig.align="center", fig.width=6, fig.height=6, fig.path="Figs/", echo=TRUE, warning=FALSE, message=FALSE, cache=F)
```

```{r, include=FALSE}
library(tidyverse);  library(GGally); library(stringr)
```

# Pre visualisation des données de chats d'australies


## Overture des 2 csv : 

```{r}
CatAusGPS = read.csv("../data/PetCatsAustraliaGPS"
                  , stringsAsFactors = F)
CatAusCarac = read.csv("../data/PetCatsAustraliaCaract"
                  , stringsAsFactors = F)

```

1er problème : tous les tableaux n'ont pas le même nombre de Variables

> nécécité de concaténer les 2 csv afin d'extraires toutes informations liées aux chats à chaque signal du tracker

```{r}
summary(CatAusCarac)

summary(CatAusGPS)
```

pour concaténer il faut vérifier que nous avons les mêmes noms de chats sur les 2 tableau

```{r}
unique(CatAusCarac$animal.id == CatAusCarac$deployment.id)
```

> Condition validée

### Pour Caracs nous avons : 

```{r}
names(CatAusCarac)
```

Nous allons garder : "animal.id","deploy.on.date","deploy.off.date","animal.comments","animal.life.stage",  "animal.reproductive.condition","animal.sex","manipulation.comments"

### Pour la localisation, on à comme variable : 

```{r}
names(CatAusGPS)
```

Nous allons garder : "event.id","timestamp","location.long","location.lat","individual.local.identifier"


## rm des variables spécifique de chaque talbleau

```{r}
#les vars qu'on garde : 
Caracs = c( "animal.id","deploy.on.date","deploy.off.date","animal.comments","animal.life.stage",  "animal.reproductive.condition","animal.sex","manipulation.comments")
GPS = c("event.id","timestamp","location.long","location.lat","individual.local.identifier")
```

## des variables sont a simplifié pour l'étude :

```{r}
CatAusCarac.shave = subset.data.frame(CatAusCarac,select =  Caracs)
CatAusGPS.shave = subset.data.frame(CatAusGPS,select =  GPS)
colnames(CatAusGPS.shave)[5] = "animal.id"
```


## Problème : la sythaxe de certaines collones sont mauvaise : 

"animal.comments", "manipulation.comments"

solution : split ces collones en nouvelles : 
"Hunt", "N.pray" et "Hrs.indors", "N.neigbours"

```{r}
NewRows = matrix(nrow = length(CatAusCarac.shave[,1]), ncol = 9)
colnames(NewRows) = c("animal.id","Hunt", "N.pray", "Hrs.indors", "N.neigbours",
                      "StartDate","StartHours","EndDate","EndHours")
```

Les patterns pour extraire les données : 

```{r}
hunt.pattern = "Hunt:\\s*(Yes|No)"
prey.pattern = "prey_p_month:\\s*(\\d+)"

hrs.pattern = "hrs_indoors:\\s*(\\d+)"
cats.pattern = "n_cats:\\s*(\\d+)"

date_pattern = "(\\d{4}-\\d{2}-\\d{2})"
time_pattern = "(\\d{2}:\\d{2}:\\d{2}\\.\\d{3})"

```

```{r}
#-----------------Utilisation d'une boucle pour appliquer le pattern à chaque ligne des caracs : -----------------# 

for (i in 1:length(CatAusCarac.shave[,1])){
    ligne = subset(CatAusCarac.shave[i,], 
                   select = c("animal.comments","manipulation.comments",
                              "deploy.on.date","deploy.off.date"))

    # usage de chaque pattern
    fooHunt = str_match(ligne[1], hunt.pattern)[2]        # chasse?
    fooPray = str_match(ligne[1], prey.pattern)[2]        # nombre de proie
    fooHrs = str_match(ligne[2], hrs.pattern)[2]          # nombre d'heure intérieure
    fooCats = str_match(ligne[2], cats.pattern)[2]        # nombre de chats côtoyées
    fooStartDate = str_match(ligne[3], date_pattern)[2]   # date de début
    fooStartHours = str_match(ligne[3], time_pattern)[2]  # heure de début
    fooEndDate = str_match(ligne[4], date_pattern)[2]     # date de fin
    fooendHours = str_match(ligne[4], time_pattern)[2]    # heure de fin
    fooID = CatAusCarac.shave[i,]$animal.id
    
    # on applique les données extraites par ligne 
    NewRows[i,] = c(fooID,fooHunt,fooPray,fooHrs,fooCats,fooStartDate,
                    fooStartHours,fooEndDate,fooendHours)
}
```


## Fusionner les 2 tables : 

coincider les 2 tables : 
```{r}
FullAusCarac = left_join(CatAusCarac.shave, data.frame(NewRows),
                         by = "animal.id") 
FullAusCarac = FullAusCarac[,-4] ;FullAusCarac = FullAusCarac[,-7]; FullAusCarac = FullAusCarac[,-2]

FullDataset.Cat = full_join(CatAusGPS.shave, FullAusCarac,
                            by = "animal.id")
```

## mettre les cases vides en NA

```{r}
FullDataset.Cat$N.neigbours[is.na(FullDataset.Cat$N.neigbours)] = "-1"
FullDataset.Cat$N.pray[is.na(FullDataset.Cat$N.pray)] = "-1"
FullDataset.Cat[FullDataset.Cat == ""] = NA
```

## print le CSV en out 

```{r}
write.csv(FullDataset.Cat, "../data/OutCatdata.csv")
```





