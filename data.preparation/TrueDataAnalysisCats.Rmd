---
title: "DataAnalysisTrueData"
author: "AMOUROUX Jan RODRIGES Camille-Astrid BRAHIMI Celia"
date: "`r Sys.Date()`"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE, eval=F}
knitr::opts_chunk$set(fig.align="center", fig.width=6, fig.height=6, fig.path="Figs/", echo=TRUE, warning=FALSE, message=FALSE, cache=TRUE)
```

Importation des librairies
```{r}
library(plotly) ; library(magrittr) ; library(factoextra) ; library(tidyverse);  library(GGally); library(stringr); library(Hmisc)
```

Chargement des données
```{r}
OutCatdata = read.csv("../data/OutCatdata.csv", stringsAsFactors = T, na.strings = '')
```

Visualisation de la répartition des données
```{r, eval= F, include= F}
# Résumé des informations du fichier
summary(OutCatdata)

# Visualisation du caractère de chasse des chats
OutCatdata %>%
  group_by(Hunt, animal.id)%>%
  count(animal.id)

# Visualisation du nombre de chats dans chaque classe
NonHunter = 0 ; Hunter = 0 ; Unknown = 0
CatList = unique(OutCatdata$animal.id)
for (i in CatList){
  line = which(OutCatdata$animal.id ==i )
  foo = OutCatdata[line[1],]$Hunt
  if (foo == "No")
    NonHunter = NonHunter +1
  if (foo == "Yes")
    Hunter = Hunter +1
  if (foo == "NA")
    Unknown = Unknown +1
}
cat("Nombre de chats : non chasseur", NonHunter, "chasseur", Hunter, "inconnu", Unknown)
cat("Pour un total de", NonHunter+Hunter+Unknown, "chats.")

# Nombre de chats dans le fichier
length(unique(OutCatdata$animal.id))

#V0
p = OutCatdata %>%
  ggplot() +
  geom_boxplot()
ggplotly(p)



#V1
# Sélectionner uniquement les colonnes numériques (hors animal.id et Hunt)
#numeric_vars <- OutCatdata %>%
#  select(where(is.numeric)) %>%
#  names()

# Pour chaque variable numérique, générer un boxplot par classe de Hunt
#plot_list <- lapply(numeric_vars, function(var) {
#  p <- ggplot(OutCatdata, aes(x = Hunt, y = .data[[var]], fill = Hunt)) +
#    geom_boxplot() +
#    labs(title = paste("Boxplot de", var, "par classe de chasse"),
#         x = "Comportement de chasse",
#         y = var) +
#    theme_minimal()
#  ggplotly(p)
#})

# Afficher le premier plot (par exemple) ou utiliser subplot() pour tous les afficher
#plot_list[[1]]  # Pour visualiser le premier
# ou
#subplot(plot_list, nrows = length(plot_list))  # pour tous les afficher dans une grille


```

## test Jan

test ou on regarde que avec du quantitatif (CF tp1 champignon)

```{r}
CatDataquali = OutCatdata[,-1:-5]
CatDataquali = (CatDataquali[,-9:-12])
# convertir les Int en facteurs
CatDataquali$N.pray = as.factor(CatDataquali$N.pray) ;
CatDataquali$N.neigbours = as.factor(CatDataquali$N.neigbours) 
```

#### modification de l'age pour ajouter un 0 au début afin de trier les chiffrs par ordre croissant : 

```{r}
CatDataquali$Hrs.indors = as.numeric(CatDataquali$Hrs.indors)


order = sprintf("%02d", -1:24)

CatDataquali$Hrs.indors = order[ CatDataquali$Hrs.indors ] %>% as.factor

```



On a des variables avec beaucoup de niveaux (nom, dates) nous allons les retirer car ralentit le temps de calcul et va génerer des règles trop spécifique (le nom surtout) 

```{r}
trimmedCatDataquali = CatDataquali[,-1]
trimmedCatDataquali = trimmedCatDataquali[,-8:-11]
```


## application du TP : 


#### 1 exemple avec N.pray pour prédire Hunt

```{r}
CatDataquali %>%
  ggtable('Hunt','N.pray',
          cells = 'row.prop', fill = "std.resid",
          cardinality_threshold=NULL)
```

première visualisation de si chasse ou pas en fonction du nombre de proie

cardinality_threshold c'est une sécurité mais nous il nou faut la retirer car on a de multiple niveaux


#### Toutes les variables pour prédire Hunt

```{r}
trimmedCatDataquali %>% ggbivariate(outcome = "Hunt",
                             explanatory = colnames(trimmedCatDataquali),
                              cardinality_threshold=NULL)
```

#### visualisation des différentes variables

stockage de Hunt dans `classes` et le tableau variables dans `Vars`

```{r}
classes = trimmedCatDataquali$Hunt

Vars = trimmedCatDataquali%>% select(-Hunt)
```


```{r, fig.width=12, fig.height=6}
par(mfrow = c(1,2))
foo=sapply(colnames(Vars), 
           function(a) 
             plot(
               table(unlist(Vars[,a]), classes), 
               main=a, las = 2,
               col = c("gray","red3","green4")
               )
           )
```

## ouvrir le jeu de donénesz quaézstitatif

```{r}

```


